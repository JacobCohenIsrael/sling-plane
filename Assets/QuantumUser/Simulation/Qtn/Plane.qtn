asset MovementConfig;

component PlaneStats {
    bool WasLaunched;
    FP ResponsiveModifier;
    FP SlingPower;
    FP DistancePassed;
    FPVector3 InitialPosition;
}

component PlaneConfig {
    AssetRef<MovementConfig> MovementConfig;
}

signal OnPlaneCompleteStop(EntityRef entity, PlayerRef player);